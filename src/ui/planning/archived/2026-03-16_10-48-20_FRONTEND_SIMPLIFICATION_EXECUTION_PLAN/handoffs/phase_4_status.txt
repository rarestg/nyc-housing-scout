Phase 4 Status
==============

Status
------
complete

Started at
----------
2026-03-16 12:15 EDT

Finished at
-----------
2026-03-16 12:22 EDT

Owned paths
-----------
- `src/ui/dashboard/app/AppShell.jsx`
- `src/ui/dashboard/routes/debug/DebugRoute.jsx`
- `src/ui/dashboard/styles/dashboard.css`
- `src/ui/dashboard/app/shell-context.jsx` if needed

Files changed
-------------
- `src/ui/dashboard/app/AppShell.jsx`
- `src/ui/dashboard/routes/debug/DebugRoute.jsx`
- `src/ui/dashboard/styles/dashboard.css`
- `src/ui/dashboard/dist/app.js` (generated by `npm run build:dashboard`)
- `src/ui/planning/archived/2026-03-16_10-48-20_FRONTEND_SIMPLIFICATION_EXECUTION_PLAN/handoffs/phase_4_status.txt`

Commands run
------------
- `npm test`
- `npm run build:dashboard`
- `npm run inspect:ui -- --port 4310`
- `node -e 'fetch("http://127.0.0.1:4310/api/dashboard/debug/runs?page=1&pageSize=5") ...'`
- `agent-browser session list`
- `agent-browser --session phase4-shell-css-smoke set viewport 1440 960`
- `agent-browser --session phase4-shell-css-smoke open 'http://127.0.0.1:4310/listings?detail=open&listingId=lst_000258'`
- `agent-browser --session phase4-shell-css-smoke snapshot -i`
- `agent-browser --session phase4-shell-css-smoke click @e11`, `click @e9`, and `click @e10` to confirm normal rail navigation across `/posts`, `/review`, and `/debug`
- `agent-browser --session phase4-shell-css-smoke open 'http://127.0.0.1:4310/debug/runs/2026-03-16T14-58-58-454Z?detail=open'`
- `agent-browser --session phase4-shell-css-smoke set viewport 820 900`
- `agent-browser --session phase4-shell-css-smoke open 'http://127.0.0.1:4310/debug?runId=2026-03-16T14-58-58-454Z&detail=open'`
- `agent-browser --session phase4-shell-css-smoke close`
- `agent-browser session list`

Blockers
--------
- none

Open questions
--------------
- none

Removed shell branches
----------------------
- Deleted `AppShell`'s `navCollapsed` state, the expand/collapse button, the conditional nav heading branch, and the conditional nav-label branch.
- Deleted the expanded-rail grid branches from `dashboard.css`; the shell now only uses the compact rail width plus the existing detail-open/detail-closed split.
- Deleted the collapsed-only CSS selectors and responsive overrides that existed only to reconcile expanded vs collapsed rail states.

CSS sections merged
-------------------
- Merged the duplicated compact-table selectors for `.listings-table-shell` and `.records-table-shell` covering caption spacing, header cell padding, body cell padding, selected-row box shadow, and compact `.cell-meta` typography.
- Merged the redundant detail action/link-pill styling by sharing one base block for `.detail-pane-actions` and `.detail-link-row` items and using container-specific color tokens for the accent variant.

Shell behavior intentionally kept
---------------------------------
- Kept the shell-slot pattern and `DashboardShellProvider` contract unchanged.
- Kept the existing detail pane open/closed behavior driven by the `detail` query param.
- Kept the compact rail visual treatment itself; this phase only removed the unused expanded branch and toggle.

Handoff summary
---------------
- `AppShell.jsx` is now permanently compact: the rail renders only the brand mark plus the four code links with existing `aria-label`/`title` support, and there is no nav toggle state left to maintain.
- `DebugRoute.jsx` now treats the route param as the only selection source. The `runId` query-state schema entry and fallback read path are gone, and a small effect strips any stale `?runId=` query param so `/debug` and `/debug/runs/:runId` stay canonical.
- `dashboard.css` no longer carries duplicate compact-vs-expanded shell rules or duplicate compact-table rules. The remaining shared table and action-pill selectors are flatter and cheaper to maintain without changing the shell's visual direction.
- Validation passed: `npm test`, `npm run build:dashboard`, and `npm run inspect:ui` all succeeded. Browser smoke checks confirmed desktop compact-rail layout, narrower-width overlay detail behavior, normal rail navigation, `/debug`, and a real `/debug/runs/2026-03-16T14-58-58-454Z?detail=open` flow.
