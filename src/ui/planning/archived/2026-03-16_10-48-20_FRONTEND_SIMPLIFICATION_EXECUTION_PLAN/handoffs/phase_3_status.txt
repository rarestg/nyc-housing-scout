Phase 3 Status
==============

Status
------
complete

Started at
----------
2026-03-16 11:56 EDT

Finished at
-----------
2026-03-16 12:04 EDT

Owned paths
-----------
- `src/storage/sqlite-storage.js`
- `src/ui/inspection-server.js` if needed
- `src/ui/dashboard/routes/listings/ListingsRoute.jsx`
- `src/ui/dashboard/routes/posts/PostsRoute.jsx`
- `test/dashboard-api.test.js`

Files changed
-------------
- `src/storage/sqlite-storage.js`
- `src/ui/dashboard/routes/listings/ListingsRoute.jsx`
- `src/ui/dashboard/routes/posts/PostsRoute.jsx`
- `test/dashboard-api.test.js`
- `src/ui/planning/archived/2026-03-16_10-48-20_FRONTEND_SIMPLIFICATION_EXECUTION_PLAN/handoffs/phase_3_status.txt`
- `src/ui/dashboard/dist/app.js` (generated by `npm run build:dashboard`)

Commands run
------------
- `node --test test/dashboard-api.test.js`
- `npm test`
- `npm run build:dashboard`
- `npm run inspect:ui -- --port 4310`
- `node -e 'fetch("http://127.0.0.1:4310/api/dashboard/listings?pageSize=100") ...'`
- `node -e 'fetch("http://127.0.0.1:4310/api/dashboard/posts?pageSize=100") ...'`
- `agent-browser --session phase3-review-links open "http://127.0.0.1:4310/listings?detail=open&listingId=lst_000258" && ... snapshot -i -s ".detail-pane-actions"`
- `agent-browser --session phase3-review-links click @e10 && ... get url`
- `agent-browser --session phase3-post-review-links open "http://127.0.0.1:4310/posts?detail=open&observationId=obs_001174" && ... snapshot -i -s ".detail-card-grid"`
- `agent-browser --session phase3-post-review-links get html ".detail-card-grid"`
- `agent-browser session list`
- `agent-browser --session phase3-review-links close`
- `agent-browser --session phase3-post-review-links close`

Blockers
--------
- none

Open questions
--------------
- none

Chosen payload field
--------------------
- `reviewLinkTarget: { queue: string, reviewId: string } | null`
- Current backend shape is concrete and lightweight:
- populated reviewable listings emit `{ queue, reviewId }`
- clean listings emit `null`

Deleted frontend helpers
------------------------
- `src/ui/dashboard/routes/listings/ListingsRoute.jsx`: deleted `deriveReviewQueue(...)`
- `src/ui/dashboard/routes/posts/PostsRoute.jsx`: deleted `deriveReviewId(...)`
- Listings and Posts now only serialize the backend `reviewLinkTarget`; they no longer compute queue or review IDs from listing facts

Handoff summary
---------------
- `src/storage/sqlite-storage.js` now computes listing review semantics once via `collectDashboardListingReviewCandidates(...)`, uses that for the Review read model, and exposes the canonical `reviewLinkTarget` on listing list/detail payloads plus linked-listing payloads in post detail.
- `src/ui/dashboard/routes/listings/ListingsRoute.jsx` now consumes `selectedListing.reviewLinkTarget` for the review drawer action instead of deriving a queue locally.
- `src/ui/dashboard/routes/posts/PostsRoute.jsx` now consumes `listing.reviewLinkTarget` for linked-listing review links instead of deriving review IDs locally.
- `test/dashboard-api.test.js` now covers both `reviewLinkTarget: null` for clean listings and `{ queue, reviewId }` for reviewable listings across storage helpers, listing detail payloads, and post linked-listing payloads.
- Full validation passed: `npm test` and `npm run build:dashboard`.
- Live smoke check used a real listing from local data: `lst_000258` / `obs_001174`. Listings drawer navigation clicked through to `/review?queue=low-confidence&detail=open&reviewId=low-confidence%3Alst_000258`.
- Posts detail rendered the same backend-provided review href in the linked-listing card DOM. `agent-browser` did not trigger SPA navigation when clicking that anchor in this environment, but the rendered href matched the canonical backend target exactly and the target review route itself loaded correctly in the Listings smoke check.
